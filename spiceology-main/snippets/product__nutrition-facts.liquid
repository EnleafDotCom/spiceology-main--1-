{% if product.metafields.custom.nf_serving_size != blank or product.metafields.custom.nf_calories != blank %}
<div>
    <div x-data="{ expanded: false }" role="region">
        <h6>
            <button x-on:click="expanded = !expanded" :aria-expanded="expanded"
                class="flex w-full items-center justify-between font-bold h-12">
                <span class="body-md">Nutrition Facts</span>
                <div
                    class="flex justify-center items-center w-10 h-10 rounded-[12px] md:hover:bg-gray-10 transition-all duration-200 ease-in-out ml-4">
                    <span x-show="expanded" aria-hidden="true" class="">{% render 'svg__caret', class: 'transform
                        -scale-y-100' %}</span>
                    <span x-show="!expanded" aria-hidden="true" class="">{% render 'svg__caret' %}</span>
                </div>
            </button>
        </h6>
        <div x-show="expanded" x-collapse>
            <div class="flex flex-col pb-4 px-3">
                <div class="flex flex-col p-6 bg-white">
                    <div class="flex flex-col items-start">
                        <div class="flex items-center gap-3">
                            <p class="body-md font-500">Serving size:</p>
                            <p class="font-mono text-13 leading-140 tracking-2">{{ product.metafields.custom.nf_serving_size }}</p>
                        </div>
                        <div class="flex items-center gap-3">
                            <p class="body-md font-500">Calories:</p>
                            <p class="font-mono text-13 leading-140 tracking-2">{{ product.metafields.custom.nf_calories }}</p>
                        </div>
                        <hr class="my-5">
                        <div class="grid grid-cols-1 lg:grid-cols-2 w-full">
                            <div class="flex flex-col items-start gap-y-1">
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Total Fat:</p>
                                    <p>{{ product.metafields.custom.nf_total_fat }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Saturated Fat:</p>
                                    <p>{{ product.metafields.custom.nf_saturated_fat }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Trans Fat:</p>
                                    <p>{{ product.metafields.custom.nf_trans_fat }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Cholesterol:</p>
                                    <p>{{ product.metafields.custom.nf_cholesterol }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Sodium:</p>
                                    <p>{{ product.metafields.custom.nf_sodium }}</p>
                                </div>
                               
                            </div>
                            <div class="flex flex-col items-start gap-y-1 lg:pl-8">
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Total Carbohydrate:</p>
                                    <p>{{ product.metafields.custom.nf_total_carbohydrate }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Dietary Fiber:</p>
                                    <p>{{ product.metafields.custom.nf_dietary_fiber }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Total Sugars:</p>
                                    <p>{{ product.metafields.custom.nf_total_sugars }}</p>
                                </div>
                                <div class="body-md flex items-end gap-1.5">
                                    <p>Protein:</p>
                                    <p>{{ product.metafields.custom.nf_protein }}</p>
                                </div>
                            </div>
                        </div>
                        {% comment %} <div class="body-sm pt-10">Not a significant source of vitamin D, calcium, iron, and potassium<br>*The % Daily Value (DV) tells you how much a nutrient in a serving of food contributes to a daily diet. 2,000 calories a day is used for general nutrition advice.</div> {% endcomment %}
                    </div>
                </div>
            </div>
        </div>
        <hr>
    </div>
</div>
{% endif %}